<h1>Editing Book</h1>

<%= form_with model: @book do |f| %>


  <h4>Titleh4</h4>
  <%= f.text_area :title %>

  <h4>Body</h4>
  <%= f.text_area :body %>

  <%= f.submit 'Update Book' %>
  <%= link_to 'Show', "/books/#{@book.id}" %> |
  <%= link_to 'Back', :back %>

<% end %>
